//jQuery

//Consider the jQuery object, the kind you get in response to code like:
$('.myClass'); 

//This object has properties like..
$('.myClass').length; // returns the number of elements in the jQuery object

//But mostly we use methods on this object...
$('.myClass').text(); // READ the content of a text node
$('.myClass').text("🤗"); // SET the content of a text node

// Similarly we have dual-purpose methods for .css() and .attr(). 
// We can add listeners with .on(). We can read values with .val(), 
// blah blah blah.







